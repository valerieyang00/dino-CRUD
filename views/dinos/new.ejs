<h2>Make a new Dino:</h2>
<!-- POST vs GET form
    GET forms put the data into the query string of the url -- in express you use the req.query (used for READ)
    POST forms put the data into the request body -- and in express we access it through the req.body (used for CREATE)-->

<form method="POST" action="/dinosaurs">
    <label for="dinoName">Name:</label>
    <input id="dinoName" type="text" name="name" />

    <label for="dinoType">Type:</label>
    <input id="dinoType" type="text" name="type" />

    <button type="submit">Create Dino</button>

</form>